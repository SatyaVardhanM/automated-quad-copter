# faces/

Place registered user face images here.

- File format: `.jpg` or `.png`
- File name = user's name (e.g. `satya.jpg`)
- The drone will use this directory to build its face recognition database at runtime.

**Example:**
```
faces/
  registered_user.jpg
  sample_user.jpg
```

Do not commit real face images to a public repository.
